'use client';
import { useEffect, useState } from 'react';
import { ArrowLeft, Bell, Check, Loader2 } from 'lucide-react';
import Link from 'next/link';
import SpotlightCard from '@/components/SpotlightCard';
import { useAuth } from '@/context/AuthContext';
import { apiGetNotifications, apiOpenNotification } from '../../lib/api';
export default function Notifications() { const { token } = useAuth(); const [rows, setRows] = useState(null); const load = async () => setRows((await apiGetNotifications(token)).notifications); useEffect(() => { load() }, [token]); const open = async id => { await apiOpenNotification(token, id); await load() }; return <div className="max-w-3xl mx-auto space-y-5 pb-12"><Link href="/dashboard/procurement" className="flex items-center gap-2 text-xs font-black text-slate-500"><ArrowLeft className="w-4 h-4" /> Procurement</Link><div><p className="text-xs font-black uppercase tracking-widest text-[#c8834a]">Notifications</p><h1 className="text-3xl font-black mt-1">Procurement Inbox</h1><p className="text-xs text-slate-500 mt-1">Mock of the notification list/open contract. SSE can be connected later without changing this UI.</p></div>{!rows ? <div className="p-12 text-center"><Loader2 className="w-7 h-7 animate-spin mx-auto text-[#c8834a]" /></div> : rows.map(n => <SpotlightCard key={n.id} className="p-4 rounded-2xl bg-white" spotlightColor="rgba(200,131,74,.04)" style={{ border: '1px solid rgba(200,131,74,.15)' }}><div className="flex gap-3"><Bell className="w-5 h-5 text-[#c8834a] mt-1" /><div className="flex-1"><div className="flex justify-between gap-3"><b className="text-sm">{n.title}</b>{n.read && <Check className="w-4 h-4 text-green-600" />}</div><p className="text-xs text-slate-500 mt-1">{n.message}</p>{!n.read && <button onClick={() => open(n.id)} className="mt-3 px-3 py-1.5 rounded-lg bg-slate-100 text-[10px] font-black">Open</button>}</div></div></SpotlightCard>)}</div> }
